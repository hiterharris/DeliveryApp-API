const { usersData } = require('./users');
const { productsData, beerData, wineData, tobaccoData, vapeData } = require('./products');

module.exports = { usersData, productsData, beerData, wineData, tobaccoData, vapeData }